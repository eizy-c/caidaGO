name: caida_server
description: "Servidor WebSocket en la nube para CaidaGO - Salas y partidas multijugador online."
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ^3.0.0

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  shelf_web_socket: ^2.0.0
  web_socket_channel: ^2.4.1

dev_dependencies:
  lints: ^3.0.0
